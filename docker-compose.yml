services:
  db:
    image: mysql:8.0
    container_name: unistats-mysql
    environment:
      MYSQL_DATABASE: unistats
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3307:3306"
    volumes:
      - unistats_db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "--silent"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: unistats-backend:1.0.1
    container_name: unistats-backend
    environment:
      DATABASE_URL: mysql+pymysql://root@db:3306/unistats
      JWT_SECRET_KEY: change-this-in-production
    ports:
      - "5000:5000"
    depends_on:
      db:
        condition: service_healthy
    restart: on-failure

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:5000
    image: unistats-frontend:1.0.1
    container_name: unistats-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  unistats_db_data:
